DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=
DB_PORT=

# Optional: clustering still works without this key; only behaviour assessment is omitted.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TIMEOUT_SECONDS=45

# Required only for /admin/models/* operations. Keep the real value in .env.
MODEL_PATH=./saved_models/
MODEL_ADMIN_API_KEY=
